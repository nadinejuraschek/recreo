// DEPENDENCIES
import styled from 'styled-components';

export const Container = styled.form.attrs(() => ({
  className: 'form',
}))`
  display: flex;
  flex-direction: column;
  align-items: center;
`;
