export interface TitleProps {
  children: any;
  color?: string;
  marginBottom?: number;
  size?: string;
}

export interface StyledTitleProps {
  color: string;
  marginBottom: number;
  size: string;
}
