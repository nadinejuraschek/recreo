export interface TitleProps {
  children: React.ReactNode;
  color?: string;
  marginBottom?: number;
  size?: string;
}

export interface StyledTitleProps {
  color: string;
  marginBottom: number;
  size: string;
}
